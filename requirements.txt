# Core runtime dependencies (cross-platform).
# Installs Azure brain + Azure voice + GUI.
# Optional stacks are split into dedicated files:
#   - requirements-rag.txt
#   - requirements-qwen.txt
#   - requirements-xtts.txt

openai>=1.54,<3
azure-cognitiveservices-speech>=1.42,<2
PyQt6>=6.6,<7
requests>=2.32,<3
beautifulsoup4>=4.12,<5
pygame>=2.5,<3
python-dotenv>=1.0,<2
